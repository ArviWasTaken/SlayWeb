{
  "name": "SlayWeb",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "web-dev-server --node-resolve --open --watch --root-dir dist"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.6",
    "typescript": "^5.5.3"
  },
  "private": true
}
